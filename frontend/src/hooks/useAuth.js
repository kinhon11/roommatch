import { useAuth } from '../context/AuthContext';

/**
 * Hook để dùng auth context dễ dàng trong components
 * Re-export từ context để các components chỉ cần import từ hooks/
 */
export { useAuth };
